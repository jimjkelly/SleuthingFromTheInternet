          <div class="span12 offset1">
                <p>Sleuthing from the Internet seeks to make the job of monitoring seismic data to detect things that go boom much easier.  Below is a list of the 1000 most recent events (view <a href="<%= url('/events') %>">all</a>):</p>
                <br>
                <%= erb :'partials/map_interface' %>
          </div>