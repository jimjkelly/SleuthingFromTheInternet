                                ID: <%= link(url('/event/' + event.id.to_s), event.id.to_s) %> MAG: <%= event.mag %> DEPTH: <%= event.depth %> (<%= link('http://' + event.source + event.url, event.source + ' source page') %>)<br>
