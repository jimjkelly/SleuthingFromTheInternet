          <div class="span10 offset1">
            <p>Below is the information we have on <%= @event.event_id %> (<%= link(url('/event/' + @event.event_id + '.json'), 'JSON') %>):</p>
            <dl class="dl-horizontal text-overflow">
                <% @event.attributes.each do |attribute| %>
                    <div class="row">
                        <dt><%= attribute[0] %></dt>
                        <dd><%= attribute[1] %></dd>
                    </div>
                <% end %>
            </dl>
          </div>
            